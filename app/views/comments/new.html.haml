.container
  %h2.page-ttl New Comment
  %ul
    - @comment.errors.full_messages.each do |message|
      %li
        = message
  =form_with(model: @comment, url: task_comments_path(@comment.task), method: 'post', local: true) do |f|

    %div
      =f.text_area :content, class: 'text new-board-txt'

    = f.submit '保存', class: 'btn-primary'
